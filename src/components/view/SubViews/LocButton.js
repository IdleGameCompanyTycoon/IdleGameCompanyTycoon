import React from 'react';

const LocButton = (props) => {

  // TODO: Add functionality and styles in style css
  return (
    <div className='App-loc-button' onClick={props.clickLocButton}>
    </div>
  )
}

export default LocButton;
